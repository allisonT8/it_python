from banner import banner
import random
#1. get zipcode working
#2. have is reset
#3. then not a zipcode in the county happend
banner("Zip Code Sorter","Allison.T")

zipcode = input("Please enter a zipcode: ")
county =
awsers = input(f"The zipcode {zipcode} is for {county}")

retry = input("Would you like to enter another zipcode (Y/N)?: ")